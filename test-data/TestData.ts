export interface TestData {
    validUser: {
        username: string;
        password: string;
    };

    checkoutUser: {
        firstName: string;
        lastName: string;
        postalCode: string;
    };
}